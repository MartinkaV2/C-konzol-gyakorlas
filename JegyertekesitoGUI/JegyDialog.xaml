<Window x:Class="JegyertekesitoGUI.JegyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Jegy"
        Height="230" Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Név -->
        <Label Grid.Row="0" Grid.Column="0" Content="Név:" VerticalAlignment="Center"/>
        <TextBox x:Name="txtNev" Grid.Row="0" Grid.Column="1"
                 Margin="0,4" Padding="4,2"/>

        <!-- Ár -->
        <Label Grid.Row="1" Grid.Column="0" Content="Ár (Ft):" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAr" Grid.Row="1" Grid.Column="1"
                 Margin="0,4" Padding="4,2"/>

        <!-- Darabszám -->
        <Label Grid.Row="2" Grid.Column="0" Content="Darabszám:" VerticalAlignment="Center"/>
        <TextBox x:Name="txtDarabszam" Grid.Row="2" Grid.Column="1"
                 Margin="0,4" Padding="4,2"/>

        <!-- Gombok -->
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnMentes" Content="Mentés" Width="80" Margin="0,0,8,0"
                    Click="btnMentes_Click"
                    IsDefault="True"/>
            <Button x:Name="btnMegse" Content="Mégse" Width="80"
                    Click="btnMegse_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
